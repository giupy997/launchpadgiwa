# Copy to .env and fill in. NEVER commit .env.
# Private key of the deployer account (GIWA Sepolia test account).
PRIVATE_KEY=
# Optional: fee treasury address (defaults to deployer if unset).
TREASURY=
